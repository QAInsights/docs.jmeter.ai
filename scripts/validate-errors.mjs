/**
 * @deprecated All error playbook validations and FAQ schema coverage checks
 * have been migrated directly into `tests/unit/schema-validation.test.mjs`
 * and `tests/unit/seo-validation.test.mjs` (executed during `pnpm run lint` and `npm test`).
 */

