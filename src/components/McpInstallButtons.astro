---
/**
 * One-click MCP install buttons for VS Code, VS Code Insiders, and Cursor,
 * plus a "copy mcp.json config" action. Deeplink formats:
 *   VS Code:     vscode:mcp/install?<urlencoded {"name","type":"http","url"}>
 *   Insiders:    vscode-insiders:mcp/install?<same>
 *   Cursor:      cursor://anysphere.cursor-deeplink/mcp/install?name=<n>&config=<base64 {"url"}>
 */
const SERVER_NAME = 'jmeter-docs';
const ENDPOINT = 'https://docs.jmeter.ai/api/mcp';

const vscodeConfig = JSON.stringify({ name: SERVER_NAME, type: 'http', url: ENDPOINT });
const cursorConfig = JSON.stringify({ url: ENDPOINT });

const vscodeUrl = `vscode:mcp/install?${encodeURIComponent(vscodeConfig)}`;
const cursorUrl = `cursor://anysphere.cursor-deeplink/mcp/install?name=${SERVER_NAME}&config=${Buffer.from(cursorConfig).toString('base64')}`;
const mcpJson = JSON.stringify({ mcpServers: { [SERVER_NAME]: { url: ENDPOINT } } }, null, 2);
---

<div class="mcp-install" data-mcp-install>
  <a href={vscodeUrl} class="mcp-install-btn mcp-install-primary" data-mcp-deeplink="vscode">
    <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M17.583 2.057a1.6 1.6 0 0 0-.77.136L7.72 7.63a.8.8 0 0 0-.364.539L4.7 18.5a.8.8 0 0 0 1.444.572l2.19-2.92a.8.8 0 0 0 .093-.149l8.39-6.292a.8.8 0 0 1 .97.046l3.04 2.433a.8.8 0 0 0 1.283-.63V3.47a.8.8 0 0 0-.933-.79l-3.594-.623zM8.5 9.438 16.15 3.7l3.33.579-9.814 7.36L8.5 9.438zM6.9 16.86l2.03-2.706 1.355 2.033-2.61 3.48-.775-2.807z"/></svg>
    Add to VS Code
  </a>
  <a href={cursorUrl} class="mcp-install-btn" data-mcp-deeplink="cursor">
    <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M4 2h12l4 4v14a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2zm12 1.5V7h3.5L16 3.5zM8 10h8v1.5H8V10zm0 4h8v1.5H8V14z"/></svg>
    Add to Cursor
  </a>
  <button
    type="button"
    class="mcp-install-btn"
    data-mcp-copy-config
    data-mcp-target="antigravity"
    data-mcp-config={mcpJson}
    title="Copy config for Antigravity mcp_config.json"
    aria-label="Copy MCP config for Antigravity"
  >
    <svg data-copy-icon viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2a10 10 0 1 0 10 10A10 10 0 0 0 12 2zm0 18a8 8 0 1 1 8-8 8 8 0 0 1-8 8zm-1-13v5.59l4.24 4.24 1.42-1.42-3.66-3.65V7z"/></svg>
    <svg data-check-icon hidden viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/></svg>
    <span data-copy-text>Copy for Antigravity</span>
    <span class="sr-only" aria-live="polite" data-copy-status></span>
  </button>
  <button
    type="button"
    class="mcp-install-btn"
    data-mcp-copy-config
    data-mcp-target="devin"
    data-mcp-config={mcpJson}
    title="Copy MCP config for Devin"
    aria-label="Copy MCP config for Devin"
  >
    <svg data-copy-icon viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M13 3 4 14h6l-1 7 9-11h-6l1-7z"/></svg>
    <svg data-check-icon hidden viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/></svg>
    <span data-copy-text>Copy for Devin</span>
    <span class="sr-only" aria-live="polite" data-copy-status></span>
  </button>
  <button
    type="button"
    class="mcp-install-btn"
    data-mcp-copy-config
    data-mcp-target="mcp_json"
    data-mcp-config={mcpJson}
    aria-label="Copy mcp.json configuration"
  >
    <svg data-copy-icon viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/></svg>
    <svg data-check-icon hidden viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/></svg>
    <span data-copy-text>Copy mcp.json</span>
    <span class="sr-only" aria-live="polite" data-copy-status></span>
  </button>
</div>

<script>
  (() => {
    if (window.__jmeterMcpInstallReady) return;
    window.__jmeterMcpInstallReady = true;

    document.addEventListener('click', async (event) => {
      const target = event.target;
      if (!(target instanceof Element)) return;

      const deeplink = target.closest('[data-mcp-deeplink]');
      if (deeplink) {
        try {
          window.gtag?.('event', 'deeplink_click', {
            deeplink_target: deeplink.getAttribute('data-mcp-deeplink'),
            page: window.location.pathname,
          });
        } catch { /* analytics only */ }
        return;
      }

      const copyBtn = target.closest('[data-mcp-copy-config]');
      if (copyBtn) {
        const text = copyBtn.querySelector('[data-copy-text]');
        const defaultLabel = text?.textContent || 'Copy';
        const status = copyBtn.querySelector('[data-copy-status]');
        const copyIcon = copyBtn.querySelector('[data-copy-icon]');
        const checkIcon = copyBtn.querySelector('[data-check-icon]');
        try {
          await navigator.clipboard.writeText(copyBtn.getAttribute('data-mcp-config') || '');
          if (text) text.textContent = 'Copied';
          if (status) status.textContent = 'Copied MCP config';
          if (copyIcon) copyIcon.hidden = true;
          if (checkIcon) checkIcon.hidden = false;
          try {
            window.gtag?.('event', 'mcp_copy', {
              copy_target: copyBtn.getAttribute('data-mcp-target') || 'mcp_json',
            });
          } catch { /* analytics only */ }
          window.setTimeout(() => {
            if (text) text.textContent = defaultLabel;
            if (status) status.textContent = '';
            if (copyIcon) copyIcon.hidden = false;
            if (checkIcon) checkIcon.hidden = true;
          }, 1800);
        } catch {
          if (status) status.textContent = 'Copy failed';
        }
      }
    });
  })();
</script>

<style>
  .mcp-install {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    margin: 1rem 0;
  }

  .mcp-install-btn {
    appearance: none;
    -webkit-appearance: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    box-sizing: border-box;
    min-height: 2.2rem;
    padding: 0.5rem 0.95rem;
    border-radius: 9999px;
    border: 1px solid var(--sl-color-gray-5);
    background: transparent;
    color: var(--sl-color-gray-2);
    font: inherit;
    font-family: inherit;
    font-size: 0.85rem;
    font-weight: 600;
    line-height: 1;
    letter-spacing: 0;
    text-align: center;
    text-decoration: none;
    cursor: pointer;
    transition: color 150ms, border-color 150ms, background-color 150ms;
  }

  .mcp-install-btn:hover {
    color: var(--sl-color-accent);
    border-color: var(--sl-color-accent);
    background: var(--sl-color-accent-low);
  }

  .mcp-install-btn:focus-visible {
    outline: 2px solid var(--sl-color-accent);
    outline-offset: 2px;
  }

  .mcp-install-primary {
    color: var(--sl-color-accent);
    border-color: var(--sl-color-accent);
  }

  .mcp-install-btn svg {
    width: 1rem;
    height: 1rem;
    flex-shrink: 0;
  }

  .mcp-install-btn svg[hidden] {
    display: none;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
